/**
 * ContactSupportSection - お問い合わせセクション
 *
 * 新デザイン構築のため一時的に空になっています。
 * 元の実装は docs/CURRENT_LANDING_PAGE_BACKUP.md を参照してください。
 */

import type { Locale } from '../../i18n/config';

type Props = {
  locale?: Locale;
}

export function ContactSupportSection({ locale = 'ja' }: Props) {
  return null;
}

# 記憶の引き継ぎと先読み

生成方法: built-in imagegen（図2をスタイル参照）
出力: 03-memory-and-prefetch.png

## 使用プロンプト

Create a Japanese technical blog infographic matching the referenced blue/orange diagram's visual style (white, pale blue rounded panels, navy bold large text, orange accents, compact flat icons). Landscape 3:2, large readable type, concise labels, no fine print. TWO horizontal panels stacked vertically, clear left-to-right flow within each.
TOP PANEL heading 「① 記憶を次の応答へ」. Left card with small chat/book icon: 「会話履歴」 newline 「事実・現在の場面」. Arrow to central pale orange card with open box icon and exact example 「箱は開いている」 newline 「中身は机にある」. Arrow to right blue card with AI icon 「次の返答AIへ」. Below these cards one large readable line 「残すのは、命令ではなく動作後の状態」. Then short clearly readable note 「文章上の記憶。映像を見て確認した結果ではない」. Ensure example is presented as an example of state, no implication conversation history is reduced solely to box state.
BOTTOM PANEL heading 「② 映像と返答準備を並行する」. Two horizontal timeline lanes aligned to a shared vertical dotted marker near right, labeled 「生成側の音声終了」. Upper blue lane label 「Director」 and long bar 「現在の応答の映像を生成」 extending up to marker, then arrow to far-right box 「次の応答を送信」. Lower orange lane label 「アプリ」 and aligned long bar 「次の返答 → 音声合成 → アップロード」, with endpoint 「準備完了」 clearly BEFORE dotted marker, then thin line waits until marker and merges into rightmost send box. Mark right send box subtitle 「準備済みなら」. Below timeline a separate small full-width pale panel with a small motion icon and concise text 「準備が間に合わないとき → 待機指示で活動を継続」. Do NOT call marker playback complete. Do NOT suggest next speech gets sent before previous generation audio ends. No invented timing durations. Diagrams must remain readable on a phone, favor large labels over decoration. Add no global title, no redundant legend. Use small icons, not large decorative illustrations. All Japanese exact.
